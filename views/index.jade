extends layout

block content
  h1= title
  p Welcome to #{title}
  if user
    p You are logged in as: #{user.username}
    if(user.isAdmin)
      p You are an Admin!
  else
    p Please #[a(href="/u/login") login] to play